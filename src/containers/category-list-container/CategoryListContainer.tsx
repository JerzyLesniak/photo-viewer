import * as React from 'react';

import CategoryList from "../../components/category-list/CategoryList";

export default class CategoryListContainer extends React.Component {
  render () {
    return (
      <CategoryList/>
    );
  }
}